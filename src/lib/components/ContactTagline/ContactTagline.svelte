<p class="contact-tagline">
	<span><strong>Want to work together?</strong></span>
	<span>
		Set up a <a href="#">discovery call</a> or send me an
		<a href="mailto:brad.waropay@gmail.com">message</a>.
	</span>
</p>

<style lang="scss">
	.contact-tagline {
		display: flex;
		flex-wrap: wrap;
		gap: var(--spacing-micro-slim);
		font-size: slopeIntercept(
			var(--font-text-size-lg),
			var(--font-text-size-xl),
			$breakpointMinimum,
			$breakpointContainer
		);
		text-wrap: balance;
	}
</style>
